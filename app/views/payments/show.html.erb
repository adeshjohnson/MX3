<% for column in Payments.content_columns %>
    <p>
      <b><%= column.human_name %>:</b> <%= h @payments.send(column.name) %>
    </p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @payments %> |

