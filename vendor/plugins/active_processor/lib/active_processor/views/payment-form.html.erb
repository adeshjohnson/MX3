<form action='<%= post_to("") %>' method='post'>
  <ul>
    <% each_field_for(:form) do |name, options| %>
      <li>
        <%= FormHelper.label(@engine, @gateway, name, options) %>
        <%= FormHelper.input(self, name, options) %>
    </li>
  <% end %>
  </ul>
</form>
